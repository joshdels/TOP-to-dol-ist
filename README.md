# TOP-to-dol-ist

This is a todo list that focuses in classes and webpacking

---

this uses webpack and JS OOP such as classes

1. ill be starting with the psuedo and instruction how to do it

---

### how to start
ensure you have a `webpack.config.js` established

```
npm init -y --init-type=module
npm install --save-dev wepback webpack-cli html-wepback-plugin styles-loader css-loader html-loader webpack-dev-server

# run
npx webpack
npx webpack serve
```
